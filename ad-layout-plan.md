# SmartCalc 未来 AdSense 广告位布局规划

**版本：** 2026-08-14  
**目标：** 在不遮挡计算器核心操作、不诱导误点击、不破坏移动端阅读体验的前提下，为审核通过后的 AdSense 接入预留清晰、可控的广告位置。

## 一、总体原则

SmartCalc 的核心产品是“输入数据 → 点击计算 → 阅读结果”。广告必须作为独立的内容块存在，不能进入计算器表单内部，也不能覆盖、挤压或贴近输入框、下拉框、计算按钮、重置按钮、复制按钮和结果卡片。Google 官方建议广告应服务于用户体验、与内容清晰区分，并使用响应式广告单元；广告标签只能使用“Advertisements”或“Sponsored links”等不误导用户的表达。[1]

移动端尤其采用**正常文档流布局**：广告加载后只会把后续内容向下推移，绝不使用 `position: fixed`、`position: sticky`、浮层、底部悬浮条、弹出层或覆盖式容器。这样可以确保用户点击计算按钮时，广告不会突然出现在按钮下方或遮挡结果。

## 二、计算器详情页推荐结构

| 顺序 | 页面区域 | 是否放广告 | 设计要求 |
|---|---|---:|---|
| 1 | 页面标题、说明、面包屑 | 否 | 保持用户明确知道当前工具和用途。 |
| 2 | 计算器输入表单 | 否 | 输入框、单位选择、精度选择和“Calculate”按钮周围保持纯净。 |
| 3 | 结果卡片与复制/重置操作 | 否 | 结果首次出现的位置不放广告；避免用户把广告误认为结果或操作反馈。 |
| 4 | 结果说明、公式和示例 | 否 | 先让用户完成任务并理解结果。 |
| 5 | 第一个广告容器 | 可放 | 置于结果说明或公式内容之后，作为独立区块。 |
| 6 | FAQ | 可放 | 建议只在较长内容页放置一个中段广告，不要拆散连续问答。 |
| 7 | Related Tools | 可放 | 广告与相关工具链接之间保留清晰间隔，避免误把广告当导航。 |
| 8 | 页脚 | 否 | 不在隐私、条款、联系入口附近放置广告。 |

**推荐默认方案：** 计算器页每页最多一个手动广告位，位于“结果 + 公式 + 示例”之后、FAQ 之前或 FAQ 之后。对内容较短的工具页，可以完全不放手动广告，等待 AdSense 审核通过并观察 Auto ads 的实际表现后再决定。

## 三、移动端布局规范

移动端宽度以 320px、375px、390px 和 430px 为强制验收范围。广告采用响应式 display unit，容器使用独立的块级区域，不放在 `.calculator-form`、`.result-card` 或任何操作按钮的父容器内部。

建议结构如下：

```text
[页面标题与说明]
[输入表单]
[Calculate / Reset]
[结果卡片]
[结果说明]
[公式]
[示例]

[Advertisement]

[FAQ]
[Related calculators]
[页脚]
```

广告容器应预留稳定高度，避免广告请求返回后产生明显的布局跳动。建议至少保留一个不影响核心流程的占位区，并通过 `min-height` 与响应式断点控制；具体高度以 AdSense 实际返回的响应式单元为准，不应硬编码一个会裁剪广告的固定高度。

Google 的历史官方最佳实践提出，移动端可考虑 320×100 响应式展示形式，同时建议广告与内容保持足够距离以减少误点击风险。[2] 对 SmartCalc 而言，计算器操作区是高交互区域，因此采用更保守规则：广告容器与最近的按钮、输入控件或可点击导航至少保持 **150 CSS 像素的视觉距离**；如果页面结构无法自然达到该距离，则不在该位置放广告。

## 四、桌面端布局规范

桌面端可以在计算器详情页使用两列结构：左侧为计算器和正文，右侧为“On this page”和“Related tools”。广告不进入计算器主操作卡片。若使用侧栏广告，应仅在宽度至少 1100px 时显示，并与侧栏导航、相关工具链接保持清晰区分；在较窄桌面窗口自动隐藏侧栏广告，而不是压缩主表单。

推荐优先级如下：

1. **首选：** 正文中段的独立响应式广告块，位于公式/示例之后。
2. **次选：** 宽桌面侧栏中的独立广告块，位于导航和相关工具之后，带明确广告标签。
3. **不推荐：** 首屏标题下方紧贴搜索或表单的广告，因为会把用户注意力从工具任务转移出去。
4. **禁止：** 固定在底部、覆盖结果、覆盖按钮或跟随滚动遮挡正文的广告。

## 五、广告标签和视觉区分

广告外层必须包含清晰的 `Advertisement` 标签，并与 SmartCalc 的计算器卡片使用不同的视觉层级。不要让广告容器使用与“Calculate”“Copy”“Related calculators”相同的按钮样式、边框样式或图标语言；不要在广告旁放置箭头、下载、复制或下一步等容易产生导航误解的元素。Google 官方要求广告应看起来像广告，且不应被误认为菜单、导航或下载链接。[1]

建议广告区块采用：

```text
────────────────────────
Advertisement
[响应式广告单元]
────────────────────────
```

广告标签使用普通文本，不使用“Recommended answer”“Useful result”“Continue”等误导性标题。不要在广告旁放图片或装饰元素模拟广告内容，也不要主动提示用户点击广告。

## 六、加载、同意和失败处理

真实广告位只有在 AdSense 账号获批、Google CMP 已生效、`ads.txt` 可访问且站点通过线上检查后才启用。需要用户同意的广告请求必须由 Google Privacy & Messaging 与 Consent Mode 的状态控制；拒绝或未选择时不能加载不应加载的个性化广告请求。

广告加载失败、被拦截或没有填充时，容器应保持安静，不显示“点击重试”“支持我们点击广告”等文字，也不应留下大块空白。可以收缩到一个合理的最小保留空间，但不要改变计算器输入区的尺寸或位置。

在广告真正上线前，保留 `AdSlot` 关闭开关和环境控制，例如生产环境通过明确的 `ADS_ENABLED` 配置开启，而不是依靠代码中默认开启。这样可以先部署布局和测试容器，再单独开启广告请求。

## 七、明确禁止的布局

| 禁止项 | 原因 |
|---|---|
| 底部固定广告条 | 可能遮挡移动端按钮、结果或浏览器安全区域。 |
| 计算按钮上方紧贴广告 | 高度交互区域容易产生误点击。 |
| 结果卡片内部广告 | 用户可能误把广告当作计算结果或官方解释。 |
| 弹出式广告或广告遮罩 | 干扰导航和核心任务。 |
| 广告伪装成相关工具、下载或推荐答案 | 违反清晰区分和反误导原则。 |
| 每个短页面重复多个广告 | 可能使广告多于实质内容，削弱页面价值。 |
| 随机插入导致内容跳动的广告 | 破坏移动端稳定性和可用性。 |

Google 官方政策明确禁止鼓励点击、误导性实现和可能被误认为导航的广告布局；发布者也必须持续遵守政策更新。[3]

## 八、上线前验收清单

### 功能安全

- [ ] 在 320px、375px、390px、430px 宽度打开百分比、贷款、BMI 和日期计算器。
- [ ] 输入框、单位选择、Calculate、Reset、Copy 和结果内容都能直接操作。
- [ ] 广告加载前后，核心控件没有被覆盖，焦点不会跳到广告区域。
- [ ] 旋转屏幕、放大字体和系统键盘弹出时，广告仍处于正常文档流。
- [ ] iOS Safari、Android Chrome 和桌面 Chrome 至少各验证一次。

### 合规安全

- [ ] 广告外层有清晰的“Advertisement”标签。
- [ ] 广告不靠近导航、下载、复制或计算按钮到容易误点的程度。
- [ ] 不使用 fixed、sticky、popup、overlay 或遮罩广告。
- [ ] 广告与正文的视觉语言足够区分。
- [ ] CMP 的同意、拒绝、管理和撤回路径可用。
- [ ] `/privacy`、`/cookie-policy`、`/ads.txt` 和 AdSense publisher 配置相互一致。
- [ ] 不点击自己的广告，不要求用户点击，也不使用奖励或诱导文案。

### 性能和体验

- [ ] 广告容器预留稳定空间，避免明显 CLS。
- [ ] 广告不会阻塞首屏计算器脚本和输入操作。
- [ ] 广告请求失败时，计算器仍可完整使用。
- [ ] 每个短计算器页面默认不超过一个手动广告位。
- [ ] 先观察真实填充、CLS、退出率和误点击风险，再决定是否增加位置。

## 九、建议实施顺序

第一阶段只实现**不请求广告的布局壳**：在结果说明后加入 `Advertisement` 标签和稳定容器，验证移动端四种宽度和键盘场景。第二阶段在 AdSense 审核通过后接入一个响应式广告单元，只选择百分比、贷款和 BMI 三个流量较高且正文足够长的页面进行小范围观察。第三阶段根据实际 CLS、滚动深度、退出率和误点击风险决定是否扩展到其他页面；不要一开始全站开启多个广告位。

## References

[1]: https://support.google.com/adsense/answer/1282097?hl=en-GB "Google AdSense: Best practices for ad placement"

[2]: https://blog.google/products/adsense/adsense-best-practice-set-yourself-up-for-success/ "Google AdSense best practice: Set yourself up for success"

[3]: https://support.google.com/adsense/answer/48182?hl=en "Google AdSense Program policies"

## 调试预览开关验证（2026-08-19）

已新增 `/debug` 调试面板。开关默认关闭，开启后在本地存储 `smartcalc-ad-preview=true`，并通过自定义事件同步到页面广告壳。百分比页面验证显示 `Ad space reserved · preview only`，广告壳状态为 `preview`；页面没有 `ins.adsbygoogle`、Google Ads iframe 或 DoubleClick iframe。预览不会改变 `VITE_ADS_ENABLED`，也不会启用真实广告请求。调试页添加 `noindex,nofollow`，避免被搜索引擎收录。

追加验证：调试面板开启后，贷款页面同样显示 `Ad space reserved · preview only`，状态为 `preview`，真实广告元素数量为 0；百分比页面验证结果相同。核心表单、结果卡片和 FAQ 仍保持正常文档流。

BMI 页面验证完成：开启本地预览后显示 `Ad space reserved · preview only`，状态为 `preview`，真实广告元素数量为 0；输入框、Calculate、结果卡片和 FAQ 保持正常布局。
