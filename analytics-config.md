# SmartCalc Analytics 配置

SmartCalc 使用 GA4 Measurement ID `G-YX4PT011V`。Cloudflare Pages Production 环境变量名称必须为 `VITE_GA_MEASUREMENT_ID`。站点默认不加载 Analytics，只有用户主动选择 Allow analytics 后才加载 Google gtag.js；选择 Only necessary 时不会加载 Analytics。
