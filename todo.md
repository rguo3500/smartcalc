# SmartCalc Cloudflare 域名发布清单

- [x] 正式站点使用 `https://shenlanai.cc.cd`；项目中未发现 `YOURDOMAIN.com` 占位。
- [x] canonical、Open Graph、sitemap、robots 和部署说明已随最新版本发布。
- [x] 已完成 check、20 项测试、build、Wrangler dry-run，并在 Cloudflare 日志确认 `pnpm build` 成功。
- [x] 用户已完成 Cloudflare 发布授权并触发部署。
- [x] `shenlanai.cc.cd` 已在 Pages 中显示活动、SSL 已启用，首页 HTTPS 访问验证通过；仍有未配置 Analytics 环境变量和可清理的 Wrangler Pages 配置警告。
- [x] 已将 SmartCalc 最新检查点同步到 GitHub main，远程提交为 `8dda191`。
- [x] Pages 构建配置已确认：`pnpm build`、`dist/public`、main、根目录。等待新提交自动构建验证。
- [x] 已验证首页、Calculator、History、Business、Everyday、robots、sitemap 和 HTTPS 均可访问。 
